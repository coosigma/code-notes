[
  {
    "objectID": "pages/configure-next.html",
    "href": "pages/configure-next.html",
    "title": "Configure Next.js scaffold",
    "section": "",
    "text": "This page uses Quarto’s built-in support for Observable JS ({ojs}) to collect configuration inputs in the browser, generate a shell script as plain text, show a read-only preview, and provide a download link. The script is kept intentionally simple in Phase 1 so that it only checks Node.js, prints the chosen project name, and stops.\nAll code and comments are written in English to keep the generated artifacts consistent.\n\nimport {Inputs} from \"@observablehq/inputs\"\n\n// --- Project-level inputs ---------------------------------------------------\n\nviewof projectName = Inputs.text(\"Project name\", {\n  label: \"Project name\",\n  placeholder: \"my-next-app\",\n  value: \"my-next-app\"\n})\n\nviewof packageManager = Inputs.select([\n  \"pnpm\",\n  \"npm\",\n  \"yarn\",\n  \"bun\"\n], {\n  label: \"Package manager\",\n  value: \"pnpm\"\n})\n\n// Feature toggles: these flags do not yet change the script in Phase 1,\n// but they establish the shape of the configuration for later phases.\n\nviewof useTypeScript = Inputs.toggle({\n  label: \"Use TypeScript\",\n  value: true\n})\n\nviewof useESLint = Inputs.toggle({\n  label: \"Enable ESLint\",\n  value: true\n})\n\nviewof useTailwind = Inputs.toggle({\n  label: \"Enable Tailwind CSS\",\n  value: true\n})\n\nviewof useAppRouter = Inputs.toggle({\n  label: \"Use App Router (app/)\",\n  value: true\n})\n\nviewof useSrcDir = Inputs.toggle({\n  label: \"Use src/ directory\",\n  value: true\n})\n\nviewof importAlias = Inputs.text(\"Import alias\", {\n  label: \"Import alias (for future use)\",\n  placeholder: \"@/*\",\n  value: \"@/*\"\n})\n\n// --- Script generation (Phase 1 placeholder) -------------------------------\n\n// In Phase 1 the script only:\n// 1. Prints the Node.js version (or fails if Node.js is missing).\n// 2. Prints the target project name from the inputs above.\n// 3. Prints a placeholder line explaining that real scaffolding arrives in\n//    the next phase.\n//\n// The script is generated as a single string so it can be both previewed and\n// downloaded as a .sh file from the browser.\n\nscript = `#!/usr/bin/env bash\nset -euo pipefail\n\necho \"Checking Node.js installation...\"\nnode -v || {\n  echo \"Node.js is not available. Please install Node.js (recommended: &gt;= 18).\" &gt;&2\n  exit 1\n}\n\necho \"Target project name: ${projectName}\"\necho \"Phase 1 placeholder: no Next.js scaffolding logic yet.\"\n`\n\n// --- Read-only preview ------------------------------------------------------\n\n// Render the generated script as read-only text in a &lt;pre&gt; block. This makes\n// it easy to inspect and copy without allowing in-place edits.\npreview = html`&lt;pre style=\"padding: 1rem; border: 1px solid #ddd; border-radius: 0.25rem; background-color: #f8f9fa; max-height: 24rem; overflow: auto;\"&gt;${script}&lt;/pre&gt;`\n\n// --- Download link (Blob + &lt;a download&gt;) -----------------------------------\n\n// Create a fresh Blob and &lt;a download&gt; link for the current script contents.\n// When the script or inputs change, this cell re-runs and updates the target\n// file without requiring any server-side logic.\ndownloadLink = {\n  const blob = new Blob([script], { type: \"text/x-shellscript\" })\n  const url = URL.createObjectURL(blob)\n\n  const link = document.createElement(\"a\")\n  link.href = url\n  link.download = \"scaffold-next.sh\"\n  link.textContent = \"Download scaffold-next.sh\"\n  link.style.display = \"inline-block\"\n  link.style.marginTop = \"0.5rem\"\n\n  // Optional: simple styling so the link looks button-like.\n  link.style.padding = \"0.5rem 0.75rem\"\n  link.style.border = \"1px solid #0d6efd\"\n  link.style.borderRadius = \"0.25rem\"\n  link.style.backgroundColor = \"#0d6efd\"\n  link.style.color = \"#ffffff\"\n  link.style.textDecoration = \"none\"\n  link.style.fontFamily = \"system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif\"\n  link.style.fontSize = \"0.9rem\"\n\n  return link\n}\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\nAdjust the project name, package manager, and feature toggles as needed.\nReview the generated scaffold-next.sh content in the preview block.\nUse the “Download scaffold-next.sh” link to save the script locally.\nOn the target machine, make the script executable and run it inside an empty directory:\n\nchmod +x scaffold-next.sh\n./scaffold-next.sh\n\n\nFor Phase 1 the script will only check your Node.js installation and echo the chosen project name. Later phases will extend this script to call create-next-app with non-interactive flags, add Docker and Nginx assets, configure caching, and wire up observability."
  },
  {
    "objectID": "pages/configure-next.html#interactive-next.js-scaffold-configuration",
    "href": "pages/configure-next.html#interactive-next.js-scaffold-configuration",
    "title": "Configure Next.js scaffold",
    "section": "",
    "text": "This page uses Quarto’s built-in support for Observable JS ({ojs}) to collect configuration inputs in the browser, generate a shell script as plain text, show a read-only preview, and provide a download link. The script is kept intentionally simple in Phase 1 so that it only checks Node.js, prints the chosen project name, and stops.\nAll code and comments are written in English to keep the generated artifacts consistent.\n\nimport {Inputs} from \"@observablehq/inputs\"\n\n// --- Project-level inputs ---------------------------------------------------\n\nviewof projectName = Inputs.text(\"Project name\", {\n  label: \"Project name\",\n  placeholder: \"my-next-app\",\n  value: \"my-next-app\"\n})\n\nviewof packageManager = Inputs.select([\n  \"pnpm\",\n  \"npm\",\n  \"yarn\",\n  \"bun\"\n], {\n  label: \"Package manager\",\n  value: \"pnpm\"\n})\n\n// Feature toggles: these flags do not yet change the script in Phase 1,\n// but they establish the shape of the configuration for later phases.\n\nviewof useTypeScript = Inputs.toggle({\n  label: \"Use TypeScript\",\n  value: true\n})\n\nviewof useESLint = Inputs.toggle({\n  label: \"Enable ESLint\",\n  value: true\n})\n\nviewof useTailwind = Inputs.toggle({\n  label: \"Enable Tailwind CSS\",\n  value: true\n})\n\nviewof useAppRouter = Inputs.toggle({\n  label: \"Use App Router (app/)\",\n  value: true\n})\n\nviewof useSrcDir = Inputs.toggle({\n  label: \"Use src/ directory\",\n  value: true\n})\n\nviewof importAlias = Inputs.text(\"Import alias\", {\n  label: \"Import alias (for future use)\",\n  placeholder: \"@/*\",\n  value: \"@/*\"\n})\n\n// --- Script generation (Phase 1 placeholder) -------------------------------\n\n// In Phase 1 the script only:\n// 1. Prints the Node.js version (or fails if Node.js is missing).\n// 2. Prints the target project name from the inputs above.\n// 3. Prints a placeholder line explaining that real scaffolding arrives in\n//    the next phase.\n//\n// The script is generated as a single string so it can be both previewed and\n// downloaded as a .sh file from the browser.\n\nscript = `#!/usr/bin/env bash\nset -euo pipefail\n\necho \"Checking Node.js installation...\"\nnode -v || {\n  echo \"Node.js is not available. Please install Node.js (recommended: &gt;= 18).\" &gt;&2\n  exit 1\n}\n\necho \"Target project name: ${projectName}\"\necho \"Phase 1 placeholder: no Next.js scaffolding logic yet.\"\n`\n\n// --- Read-only preview ------------------------------------------------------\n\n// Render the generated script as read-only text in a &lt;pre&gt; block. This makes\n// it easy to inspect and copy without allowing in-place edits.\npreview = html`&lt;pre style=\"padding: 1rem; border: 1px solid #ddd; border-radius: 0.25rem; background-color: #f8f9fa; max-height: 24rem; overflow: auto;\"&gt;${script}&lt;/pre&gt;`\n\n// --- Download link (Blob + &lt;a download&gt;) -----------------------------------\n\n// Create a fresh Blob and &lt;a download&gt; link for the current script contents.\n// When the script or inputs change, this cell re-runs and updates the target\n// file without requiring any server-side logic.\ndownloadLink = {\n  const blob = new Blob([script], { type: \"text/x-shellscript\" })\n  const url = URL.createObjectURL(blob)\n\n  const link = document.createElement(\"a\")\n  link.href = url\n  link.download = \"scaffold-next.sh\"\n  link.textContent = \"Download scaffold-next.sh\"\n  link.style.display = \"inline-block\"\n  link.style.marginTop = \"0.5rem\"\n\n  // Optional: simple styling so the link looks button-like.\n  link.style.padding = \"0.5rem 0.75rem\"\n  link.style.border = \"1px solid #0d6efd\"\n  link.style.borderRadius = \"0.25rem\"\n  link.style.backgroundColor = \"#0d6efd\"\n  link.style.color = \"#ffffff\"\n  link.style.textDecoration = \"none\"\n  link.style.fontFamily = \"system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif\"\n  link.style.fontSize = \"0.9rem\"\n\n  return link\n}\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\nAdjust the project name, package manager, and feature toggles as needed.\nReview the generated scaffold-next.sh content in the preview block.\nUse the “Download scaffold-next.sh” link to save the script locally.\nOn the target machine, make the script executable and run it inside an empty directory:\n\nchmod +x scaffold-next.sh\n./scaffold-next.sh\n\n\nFor Phase 1 the script will only check your Node.js installation and echo the chosen project name. Later phases will extend this script to call create-next-app with non-interactive flags, add Docker and Nginx assets, configure caching, and wire up observability."
  },
  {
    "objectID": "index.html",
    "href": "index.html",
    "title": "Code notebook",
    "section": "",
    "text": "This site is your personal code notebook. The landing page provides a simple in-browser playground for TypeScript snippets. TypeScript source is compiled to JavaScript in the browser using the typescript compiler library and then executed. This is intended for small experiments and notes, not for untrusted code.\nAll code and comments below are written in English. You can write your explanations in any language you like.\n\nimport {Inputs} from \"@observablehq/inputs\"\n\n// --- TypeScript source input -----------------------------------------------\n\nviewof tsSource = Inputs.textarea({\n    label: \"TypeScript source\",\n    value: `// Example JavaScript snippet\\n// Edit this code and the output\\n// will update automatically.\\n\\nfunction greet(name) {\\n  return \"Hello, \" + name + \"!\"\\n}\\n\\nconst message = greet(\"world\")\\nconsole.log(message)\\n`,\n    rows: 16,\n    spellcheck: false\n})\n\n// --- Compilation to JavaScript --------------------------------------------\n\n// For maximum compatibility with the Quarto OJS runtime, this notebook\n// currently executes the source directly as JavaScript without a separate\n// TypeScript compilation step. TypeScript-only syntax (such as type\n// annotations) may therefore cause runtime errors.\ncompiled = (() =&gt; {\n    return { js: tsSource, error: null }\n})()\n\n// --- JavaScript preview ----------------------------------------------------\n\njsPreview = html`&lt;pre style=\"padding: 1rem; border: 1px solid #ddd; border-radius: 0.25rem; background-color: #f8f9fa; max-height: 18rem; overflow: auto;\"&gt;${compiled.js || \"// JavaScript output will appear here\"}&lt;/pre&gt;`\n\n// --- Execution and console capture ----------------------------------------\n\n// Execute the compiled JavaScript in a sandboxed function scope and capture\n// console.log output for display. This is for local experimentation only.\nrunResult = (() =&gt; {\n    const container = document.createElement(\"div\")\n    container.style.padding = \"1rem\"\n    container.style.border = \"1px solid #ddd\"\n    container.style.borderRadius = \"0.25rem\"\n    container.style.backgroundColor = \"#ffffff\"\n\n    if (compiled.error) {\n        container.innerHTML = `&lt;strong&gt;Compile error:&lt;/strong&gt; ${compiled.error}`\n        return container\n    }\n\n    const logs = []\n    const originalLog = console.log\n    console.log = (...args) =&gt; {\n        logs.push(args.join(\" \"))\n        originalLog(...args)\n    }\n\n    try {\n        const fn = new Function(compiled.js)\n        fn()\n        const text = logs.length\n            ? logs.join(\"\\n\")\n            : \"(no console.log output)\"\n        container.innerHTML = `&lt;strong&gt;Console output:&lt;/strong&gt;\\n&lt;pre style=\"margin-top: 0.5rem; white-space: pre-wrap;\"&gt;${text}&lt;/pre&gt;`\n    } catch (error) {\n        container.innerHTML = `&lt;strong&gt;Runtime error:&lt;/strong&gt; ${error}`\n    } finally {\n        console.log = originalLog\n    }\n\n    return container\n})()\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\nEdit the TypeScript code in the “TypeScript source” textarea.\nThe compiled JavaScript will appear in the preview block.\nThe code is executed automatically and the captured console.log output is shown in the “Console output” box.\nUse this page as a personal scratchpad for small TypeScript experiments and notes. For real projects, copy working snippets into your main repository or a dedicated playground project."
  },
  {
    "objectID": "index.html#interactive-typescript-notebook-landing-page",
    "href": "index.html#interactive-typescript-notebook-landing-page",
    "title": "Code notebook",
    "section": "",
    "text": "This site is your personal code notebook. The landing page provides a simple in-browser playground for TypeScript snippets. TypeScript source is compiled to JavaScript in the browser using the typescript compiler library and then executed. This is intended for small experiments and notes, not for untrusted code.\nAll code and comments below are written in English. You can write your explanations in any language you like.\n\nimport {Inputs} from \"@observablehq/inputs\"\n\n// --- TypeScript source input -----------------------------------------------\n\nviewof tsSource = Inputs.textarea({\n    label: \"TypeScript source\",\n    value: `// Example JavaScript snippet\\n// Edit this code and the output\\n// will update automatically.\\n\\nfunction greet(name) {\\n  return \"Hello, \" + name + \"!\"\\n}\\n\\nconst message = greet(\"world\")\\nconsole.log(message)\\n`,\n    rows: 16,\n    spellcheck: false\n})\n\n// --- Compilation to JavaScript --------------------------------------------\n\n// For maximum compatibility with the Quarto OJS runtime, this notebook\n// currently executes the source directly as JavaScript without a separate\n// TypeScript compilation step. TypeScript-only syntax (such as type\n// annotations) may therefore cause runtime errors.\ncompiled = (() =&gt; {\n    return { js: tsSource, error: null }\n})()\n\n// --- JavaScript preview ----------------------------------------------------\n\njsPreview = html`&lt;pre style=\"padding: 1rem; border: 1px solid #ddd; border-radius: 0.25rem; background-color: #f8f9fa; max-height: 18rem; overflow: auto;\"&gt;${compiled.js || \"// JavaScript output will appear here\"}&lt;/pre&gt;`\n\n// --- Execution and console capture ----------------------------------------\n\n// Execute the compiled JavaScript in a sandboxed function scope and capture\n// console.log output for display. This is for local experimentation only.\nrunResult = (() =&gt; {\n    const container = document.createElement(\"div\")\n    container.style.padding = \"1rem\"\n    container.style.border = \"1px solid #ddd\"\n    container.style.borderRadius = \"0.25rem\"\n    container.style.backgroundColor = \"#ffffff\"\n\n    if (compiled.error) {\n        container.innerHTML = `&lt;strong&gt;Compile error:&lt;/strong&gt; ${compiled.error}`\n        return container\n    }\n\n    const logs = []\n    const originalLog = console.log\n    console.log = (...args) =&gt; {\n        logs.push(args.join(\" \"))\n        originalLog(...args)\n    }\n\n    try {\n        const fn = new Function(compiled.js)\n        fn()\n        const text = logs.length\n            ? logs.join(\"\\n\")\n            : \"(no console.log output)\"\n        container.innerHTML = `&lt;strong&gt;Console output:&lt;/strong&gt;\\n&lt;pre style=\"margin-top: 0.5rem; white-space: pre-wrap;\"&gt;${text}&lt;/pre&gt;`\n    } catch (error) {\n        container.innerHTML = `&lt;strong&gt;Runtime error:&lt;/strong&gt; ${error}`\n    } finally {\n        console.log = originalLog\n    }\n\n    return container\n})()\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\nEdit the TypeScript code in the “TypeScript source” textarea.\nThe compiled JavaScript will appear in the preview block.\nThe code is executed automatically and the captured console.log output is shown in the “Console output” box.\nUse this page as a personal scratchpad for small TypeScript experiments and notes. For real projects, copy working snippets into your main repository or a dedicated playground project."
  },
  {
    "objectID": "pages/ts-notes.html",
    "href": "pages/ts-notes.html",
    "title": "TypeScript notebook",
    "section": "",
    "text": "This page provides a simple in-browser playground for TypeScript snippets. The TypeScript source is compiled to JavaScript in the browser using the typescript compiler library and then executed. This is intended for small experiments and notes, not for untrusted code.\nAll code and comments below are written in English. You can write your explanations in any language you like.\n\nimport {Inputs} from \"@observablehq/inputs\"\n\n// --- TypeScript source input -----------------------------------------------\n\nviewof tsSource = Inputs.textarea({\n  label: \"TypeScript source\",\n  value: `// Example JavaScript snippet\\n// Edit this code and the output\\n// will update automatically.\\n\\nfunction greet(name) {\\n  return \"Hello, \" + name + \"!\"\\n}\\n\\nconst message = greet(\"world\")\\nconsole.log(message)\\n`,\n  rows: 16,\n  spellcheck: false\n})\n\n// --- Compilation to JavaScript --------------------------------------------\n\n// For maximum compatibility with the Quarto OJS runtime, this notebook\n// currently executes the source directly as JavaScript without a separate\n// TypeScript compilation step. TypeScript-only syntax (such as type\n// annotations) may therefore cause runtime errors.\ncompiled = (() =&gt; {\n  return { js: tsSource, error: null }\n})()\n\n// --- JavaScript preview ----------------------------------------------------\n\njsPreview = html`&lt;pre style=\"padding: 1rem; border: 1px solid #ddd; border-radius: 0.25rem; background-color: #f8f9fa; max-height: 18rem; overflow: auto;\"&gt;${compiled.js || \"// JavaScript output will appear here\"}&lt;/pre&gt;`\n\n// --- Execution and console capture ----------------------------------------\n\n// Execute the compiled JavaScript in a sandboxed function scope and capture\n// console.log output for display. This is for local experimentation only.\nrunResult = (() =&gt; {\n  const container = document.createElement(\"div\")\n  container.style.padding = \"1rem\"\n  container.style.border = \"1px solid #ddd\"\n  container.style.borderRadius = \"0.25rem\"\n  container.style.backgroundColor = \"#ffffff\"\n\n  if (compiled.error) {\n    container.innerHTML = `&lt;strong&gt;Compile error:&lt;/strong&gt; ${compiled.error}`\n    return container\n  }\n\n  const logs = []\n  const originalLog = console.log\n  console.log = (...args) =&gt; {\n    logs.push(args.join(\" \"))\n    originalLog(...args)\n  }\n\n  try {\n    const fn = new Function(compiled.js)\n    fn()\n    const text = logs.length\n      ? logs.join(\"\\n\")\n      : \"(no console.log output)\"\n    container.innerHTML = `&lt;strong&gt;Console output:&lt;/strong&gt;\\n&lt;pre style=\"margin-top: 0.5rem; white-space: pre-wrap;\"&gt;${text}&lt;/pre&gt;`\n  } catch (error) {\n    container.innerHTML = `&lt;strong&gt;Runtime error:&lt;/strong&gt; ${error}`\n  } finally {\n    console.log = originalLog\n  }\n\n  return container\n})()\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\nEdit the TypeScript code in the “TypeScript source” textarea.\nThe compiled JavaScript will appear in the preview block.\nThe code is executed automatically and the captured console.log output is shown in the “Console output” box.\nUse this page as a personal scratchpad for small TypeScript experiments and notes. For real projects, copy working snippets into your main repository or a dedicated playground project."
  },
  {
    "objectID": "pages/ts-notes.html#interactive-typescript-notebook-browser-only",
    "href": "pages/ts-notes.html#interactive-typescript-notebook-browser-only",
    "title": "TypeScript notebook",
    "section": "",
    "text": "This page provides a simple in-browser playground for TypeScript snippets. The TypeScript source is compiled to JavaScript in the browser using the typescript compiler library and then executed. This is intended for small experiments and notes, not for untrusted code.\nAll code and comments below are written in English. You can write your explanations in any language you like.\n\nimport {Inputs} from \"@observablehq/inputs\"\n\n// --- TypeScript source input -----------------------------------------------\n\nviewof tsSource = Inputs.textarea({\n  label: \"TypeScript source\",\n  value: `// Example JavaScript snippet\\n// Edit this code and the output\\n// will update automatically.\\n\\nfunction greet(name) {\\n  return \"Hello, \" + name + \"!\"\\n}\\n\\nconst message = greet(\"world\")\\nconsole.log(message)\\n`,\n  rows: 16,\n  spellcheck: false\n})\n\n// --- Compilation to JavaScript --------------------------------------------\n\n// For maximum compatibility with the Quarto OJS runtime, this notebook\n// currently executes the source directly as JavaScript without a separate\n// TypeScript compilation step. TypeScript-only syntax (such as type\n// annotations) may therefore cause runtime errors.\ncompiled = (() =&gt; {\n  return { js: tsSource, error: null }\n})()\n\n// --- JavaScript preview ----------------------------------------------------\n\njsPreview = html`&lt;pre style=\"padding: 1rem; border: 1px solid #ddd; border-radius: 0.25rem; background-color: #f8f9fa; max-height: 18rem; overflow: auto;\"&gt;${compiled.js || \"// JavaScript output will appear here\"}&lt;/pre&gt;`\n\n// --- Execution and console capture ----------------------------------------\n\n// Execute the compiled JavaScript in a sandboxed function scope and capture\n// console.log output for display. This is for local experimentation only.\nrunResult = (() =&gt; {\n  const container = document.createElement(\"div\")\n  container.style.padding = \"1rem\"\n  container.style.border = \"1px solid #ddd\"\n  container.style.borderRadius = \"0.25rem\"\n  container.style.backgroundColor = \"#ffffff\"\n\n  if (compiled.error) {\n    container.innerHTML = `&lt;strong&gt;Compile error:&lt;/strong&gt; ${compiled.error}`\n    return container\n  }\n\n  const logs = []\n  const originalLog = console.log\n  console.log = (...args) =&gt; {\n    logs.push(args.join(\" \"))\n    originalLog(...args)\n  }\n\n  try {\n    const fn = new Function(compiled.js)\n    fn()\n    const text = logs.length\n      ? logs.join(\"\\n\")\n      : \"(no console.log output)\"\n    container.innerHTML = `&lt;strong&gt;Console output:&lt;/strong&gt;\\n&lt;pre style=\"margin-top: 0.5rem; white-space: pre-wrap;\"&gt;${text}&lt;/pre&gt;`\n  } catch (error) {\n    container.innerHTML = `&lt;strong&gt;Runtime error:&lt;/strong&gt; ${error}`\n  } finally {\n    console.log = originalLog\n  }\n\n  return container\n})()\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\nEdit the TypeScript code in the “TypeScript source” textarea.\nThe compiled JavaScript will appear in the preview block.\nThe code is executed automatically and the captured console.log output is shown in the “Console output” box.\nUse this page as a personal scratchpad for small TypeScript experiments and notes. For real projects, copy working snippets into your main repository or a dedicated playground project."
  }
]